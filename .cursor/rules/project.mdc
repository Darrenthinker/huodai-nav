---
description: 货代导航网项目全局规则
alwaysApply: true
---

# 货代导航网 (huodai-nav)

## 项目概述
货代行业导航网站，收录货代相关的工具、平台、资源链接。
数据来源于 WordPress 导出的 XML，经 import 页面解析后存储为 JSON。

## 技术栈
- **框架**: Next.js 15 (App Router) + React 19 + TypeScript
- **样式**: Tailwind CSS 3
- **图标**: lucide-react
- **构建模式**: 静态导出 (`output: 'export'`)，不依赖服务器端运行时
- **端口**: 3018（固定，不要更改）

## 目录结构
```
src/
  app/                   # Next.js App Router
    page.tsx             # 首页（导航主页面）
    import/page.tsx      # WordPress XML 数据导入页面
  components/            # UI 组件
    search-header.tsx    # 搜索头部
    sidebar.tsx          # 侧边栏分类导航
    category-section.tsx # 分类区块
    site-card.tsx        # 网站卡片
    contact-float.tsx    # 悬浮联系按钮
    back-to-top.tsx      # 回到顶部
    frequent-links.tsx   # 常用链接
    submit-form.tsx      # 提交表单
    promo-popup.tsx      # 推广弹窗
    ad-booking-modal.tsx # 广告预约弹窗
  data/
    categories.json      # 分类数据
    navigation.json      # 所有网站数据（核心数据文件，3000+ 条）
  lib/
    types.ts             # TypeScript 类型 (Category, Site, NavigationData)
    data.ts              # 数据读取工具函数
    ad-config.ts         # 广告配置
    click-tracker.ts     # 点击追踪
    icons.ts             # 图标映射
scripts/                 # 数据处理脚本（Node.js）
public/logos/            # 网站 logo 图片（1100+ 个，勿索引）
```

## 核心数据模型
- `Category`: { id, name, slug, icon, order }
- `Site`: { id, title, url, description, category, order, thumbnail? }
- 数据文件: `src/data/navigation.json`

## 关键约定（必须遵守）
1. **禁止修改现有 UI 界面**。界面布局和样式已定型，只完善数据和功能逻辑。
2. **端口固定 3018**，不要更改。
3. 这是**纯静态导出**项目，不能使用 getServerSideProps、API Routes 等需要服务器运行时的功能。
4. `src/data/navigation.json` 是核心数据文件，改动需谨慎。
5. 保持与现有代码风格一致。
6. 使用中文注释和文档。

## 数据流
```
WordPress XML 导出 → import 页面浏览器端解析 → navigation.json → 静态页面渲染
```

## 常用命令
- `npm run dev` — 开发服务器 (localhost:3018)
- `npm run build` — 构建（含 sitemap 生成 + 搜狗 meta 注入）
- `npm run preview` — 预览静态导出 (serve out/)

## 部署
- 静态导出到 `out/` 目录
- **仅部署到 Cloudflare Pages**（不使用 Vercel）
- 边缘规则用 Cloudflare Pages 原生文件：
  - `public/_redirects` —— 重定向（构建后输出到 `out/_redirects`）
  - `public/_headers` —— 响应头（构建后输出到 `out/_headers`）
- 修改重定向 / 安全头 / 缓存策略时，编辑上述两个文件；不要再新增 `vercel.json`
